interface:
  display_name: "PMA Code Review"
  short_description: "Stack-aware review for local diffs, PRs, and repository audits"
  default_prompt: "Use $pma-cr to review a local diff or pull request, or run a repository-wide audit, with the right stack-specific rules."
